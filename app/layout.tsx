export const metadata = {
  title: "Facebook Picture Guard Manager",
  description: "Manage your Facebook Picture Guard",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body>
        {children}
      </body>
    </html>
  );
}
